<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">
    <ImageView
        android:id="@+id/rec_verses"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/on_primary"
        android:src="@drawable/rec_verses"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="12dp"
        />
    <TextView
        android:id="@+id/text_verses"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/primary"
        tools:text="بِسْمِ اللَّهِ الرَّحْمَنِ الرَّحِيمِ"
        app:layout_constraintTop_toTopOf="@id/rec_verses"
        app:layout_constraintBottom_toBottomOf="@id/rec_verses"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="12dp"
        android:gravity="center"
        android:textSize="18dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>